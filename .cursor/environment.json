{
  "name": "PythonTrading (stock-bot)",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "portal",
      "command": "cd stock-bot && source .venv/bin/activate && PORTAL_ALLOW_OPEN_REGISTRATION=yes streamlit run portal.py --server.headless true --server.port 8501 --server.address 0.0.0.0 --browser.gatherUsageStats false"
    }
  ],
  "ports": [{ "name": "portal", "port": 8501 }]
}
