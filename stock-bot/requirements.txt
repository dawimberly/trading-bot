# Core — run_all.py, fetch_data.py, modules
alpaca-py
python-dotenv
tavily-python
yfinance
pandas
statsmodels>=0.14.0
ccxt

# (backtesting lib was for removed modules/backtest_engine.py; main backtester uses native pandas/yf sim)

# Kraken — scripts/exchange/
python-kraken-sdk

# Dashboard — dashboard.py (Streamlit backup) + dashboard_app.py (desktop)
streamlit
plotly
customtkinter
matplotlib
pillow
pystray
# Optional: pip install pyinstaller  — build with dashboard.spec

# Felix transcript sync (optional — scripts/maintenance/sync_felix_transcripts.py)
# yt-dlp
# youtube-transcript-api

# NerdMiner monitor — nerdminer/
pyserial
